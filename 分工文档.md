# 心理预约系统 — 项目分工文档

---

## 一、项目概述

| 项目名称 | 高校心理预约系统 |
|---|---|
| 技术栈 | **前端**：React + Ant Design；**后端**：Spring Cloud 微服务 + MyBatis-Plus + MySQL + Redis + RabbitMQ |
| 仓库 | 前端独立仓库 / 后端独立仓库 |
| 团队人数 | 4 人（组长 × 1，组员 × 3） |
| 开发模式 | 组长搭建骨架 → 组员各自独立开发分配到的功能模块，互不依赖 |

### 用户角色

| 角色 | 说明 |
|---|---|
| 学生 | 线上填写首访登记表，预约初访，查看预约状态 |
| 初访员 | 线下初访后录入评估结果（危机等级、问题类型、初访结论） |
| 心理助理 | 审核咨询预约，安排咨询时间/地点，管理咨询安排记录 |
| 咨询师 | 录入咨询记录，填写结案报告，申请追加咨询时段 |
| 中心管理员 | 值班管理、时间配置、基本信息维护、初访审核、统计分析 |

---

## 二、系统架构设计

### 2.1 后端微服务拆分

```
┌─────────────────────────────────────────────────────┐
│                   API Gateway (网关)                  │
│               Spring Cloud Gateway + Nacos           │
├──────────┬──────────┬──────────┬──────────┬─────────┤
│ 用户服务  │ 预约服务  │ 咨询管理  │ 统计服务   │ 通知服务  │
│          │          │  服务     │          │          │
├──────────┴──────────┴──────────┴──────────┴─────────┤
│              MySQL + Redis + RabbitMQ                │
└─────────────────────────────────────────────────────┘
```

| 微服务 | 职责 |
|---|---|
| **gateway-service** | 统一网关，认证鉴权，路由转发 |
| **user-service** | 用户管理、角色权限、咨询师/初访员基本信息维护 |
| **appointment-service** | 初访预约、预约审核、值班管理、时间配置 |
| **consultation-service** | 初访管理、咨询安排、咨询记录、追加时段、结案报告 |
| **statistics-service** | 统计分析、报表导出 |
| **notification-service** | 短信通知、消息推送 |

### 2.2 前端页面模块

```
├── 登录/注册模块
├── 学生端
│   ├── 首访登记表填写
│   ├── 知情同意书
│   ├── 初访预约（选择时间/老师）
│   └── 我的预约记录
├── 中心管理员端
│   ├── 值班管理
│   ├── 基本时间配置
│   ├── 咨询师/初访员信息维护
│   ├── 初访预约审核（含计分报警/优先排队）
│   ├── 初访预约记录管理
│   └── 统计分析（批量下载/汇总查询/Excel导出）
├── 初访员端
│   └── 初访管理（录入评估结果）
├── 心理助理端
│   ├── 咨询预约审核
│   └── 咨询安排记录管理
└── 咨询师端
    ├── 咨询记录录入
    ├── 追加咨询时段申请
    └── 结案报告填写
```

---

## 三、组长工作（骨架搭建）

> **组长先独立完成以下骨架，组员后续基于骨架各自添加功能。**

### 3.1 后端骨架

| 任务 | 说明 |
|---|---|
| **父工程 + 公共模块** | Maven 父 POM、common 模块（统一响应体、全局异常处理、工具类、Feign 接口定义） |
| **Nacos 注册中心** | 所有微服务注册到 Nacos，服务间可互相发现 |
| **Gateway 网关** | 路由转发规则、跨域配置、JWT 鉴权过滤器 |
| **认证鉴权** | JWT 登录接口、5 种角色权限模型、Spring Security 集成 |
| **数据库设计 + 建表** | 全部表结构设计、各服务数据源配置 |
| **user-service 骨架** | 用户登录/注册/信息查询基础接口 |
| **appointment-service 骨架** | 值班管理 + 时间配置 + 初访预约审核 + 预约记录管理 |
| **consultation-service 骨架** | 咨询预约审核 + 咨询安排记录 + 咨询记录 + 追加时段审批 |
| **statistics-service 骨架** | 汇总查询 + Excel 导出 + 批量下载接口 |
| **notification-service 骨架** | RabbitMQ 消息队列 + 短信发送接口 |

### 3.2 前端骨架

| 任务 | 说明 |
|---|---|
| **项目初始化** | React + Ant Design 脚手架、路由配置、Axios 封装（统一请求拦截/错误处理） |
| **登录 + 权限路由** | 登录页、根据角色渲染不同 Dashboard 菜单、路由守卫 |
| **页面布局框架** | 侧边栏 + 顶栏 + 内容区的通用布局组件 |

### 3.3 文档 + 规范

| 任务 | 说明 |
|---|---|
| **接口文档** | Swagger / Apifox 统一管理，定义好所有接口的路径、入参、返回值 |
| **数据库 ER 图 + 建表 SQL** | 组员建表 SQL 统一管理 |
| **common 模块使用说明** | 如何调用 Feign、如何使用统一响应体、全局异常处理怎么接 |

---

## 四、组员工作（基于骨架独立开发）

> **以下每个组员的全部任务，在组长骨架搭建完成后，可完全独立并行开发，无先后依赖。**

---

### 闫鑫哲 — 学生端模块（全栈），约占总工作量 **18%**

#### 后端（user-service + appointment-service）

| 编号 | 功能 | 说明 |
|---|---|---|
| Y1 | 用户管理 CRUD | 学生/初访员/心理助理/咨询师/管理员 的增删改查接口 |
| Y2 | 咨询师/初访员信息维护 | 管理员对咨询师/初访员信息的增删改（需求 1.3） |
| Y3 | 首访登记表接口 | 登记表数据模型、提交存储、计分逻辑（需求 1.7） |
| Y4 | 知情同意书接口 | 同意书记录存储（需求 1.7） |
| Y5 | 学生初访预约接口 | 查询初访老师空闲时间、提交预约、学生查看/撤销自己的预约（需求 1.7 / 1.5） |

#### 前端（学生端全部页面）

| 编号 | 页面 | 说明 |
|---|---|---|
| Y6 | 首访登记表页 | 表单 UI（含计分题目）、提交逻辑 |
| Y7 | 知情同意书页 | 在线阅读 + 电子签名确认 |
| Y8 | 初访预约页 | 展示初访老师空闲时段、选择时间、提交预约 |
| Y9 | 我的预约记录页 | 预约状态展示、撤销申请按钮 |

**交付物：** 学生端 4 个页面对应的前端组件 + 后端 5 组接口

---

### 张奇奇 — 初访管理 + 通知服务 + 咨询师端前端，约占总工作量 **17%**

#### 后端（appointment-service + consultation-service + notification-service）

| 编号 | 功能 | 说明 |
|---|---|---|
| Z1 | 初访预约记录管理接口 | 管理员改约老师/时间/地点、按学号姓名新增预约并匹配空闲老师、补录备班（需求 1.5 管理员端） |
| Z2 | 初访管理接口 | 初访员录入危机等级、问题类型、初访时间、初访结论（需求 1.8） |
| Z3 | 短信通知服务 | RabbitMQ 消息发送、短信模板、预约成功/变更/提醒通知（需求 1.7/1.9） |
| Z4 | 咨询安排记录管理接口 | 心理助理改约/新增咨询安排、匹配空闲咨询师（需求 1.10） |

#### 前端

| 编号 | 页面 | 说明 |
|---|---|---|
| Z5 | 初访员端 — 初访管理页 | 初访员录入表单（危机等级/问题类型/初访结论下拉菜单） |
| Z6 | 咨询师端 — 咨询记录页 | 每次咨询状态录入（完成/旷约/请假/脱落/结案） |
| Z7 | 咨询师端 — 追加申请页 | 咨询师提交追加时段申请、查看审批状态 |
| Z8 | 咨询师端 — 结案报告页 | 填写结案报告表单（所有必填字段） |

**交付物：** 咨询师端 3 个页面 + 初访员端 1 个页面 + 后端 4 组接口 + 短信通知服务

---

### 耿中显 — 统计分析 + 结案报告 + 部分前端，约占总工作量 **15%**

#### 后端（statistics-service + consultation-service）

| 编号 | 功能 | 说明 |
|---|---|---|
| G1 | 结案报告后端 | 报告 CRUD、Apache POI 生成 Word A4 表格（含所有字段）、存储与查询（需求 1.13） |
| G2 | 汇总查询接口 | 按学生信息/咨询师信息/问题类型多条件筛选查询（需求 1.6） |
| G3 | Excel 统计导出 | EasyExcel 按咨询师统计人次+咨询总时长并导出（需求 1.6） |
| G4 | 结案报告批量下载 | 批量选中后 Zip 打包下载（需求 1.6） |

#### 前端

| 编号 | 页面 | 说明 |
|---|---|---|
| G5 | 统计分析页 — 汇总查询 | 多条件筛选表单 + 结果表格展示 |
| G6 | 统计分析页 — Excel 导出 | 咨询师人次+时长统计展示 + 导出按钮 |
| G7 | 统计分析页 — 批量下载 | 结案报告列表 + 多选 + 打包下载按钮 |

**交付物：** 统计模块 3 个页面 + 后端 4 组接口

---

## 五、工作量占比汇总

```
组长（你）  ██████████████████████████████████████████████████  50%
闫鑫哲      ██████████████████                                18%
张奇奇      █████████████████                                 17%
耿中显      ███████████████                                   15%
```

| 成员 | 主要负责方向 | 工作量占比 |
|---|---|---|
| **组长（你）** | 前后端骨架搭建 + 认证鉴权 + 核心业务（值班/审核/8周占用/优先排队/咨询记录）+ 管理员端+助理端前端 | **50%** |
| **闫鑫哲** | 学生端全栈（登记表 → 预约 → 记录 完整链路） | **18%** |
| **张奇奇** | 初访管理 + 通知服务 + 咨询安排记录 + 咨询师端+初访员端前端 | **17%** |
| **耿中显** | 统计分析 + 结案报告 Word/Excel 生成 + 统计前端 | **15%** |

---

## 六、开发流程（无阻塞并行）

```
第1步：组长搭建前后端骨架（组员这段时间熟悉需求、学习技术栈）
   │
   ├── 后端：父工程 + Nacos + Gateway + JWT + common 模块 + 空服务模块
   └── 前端：React 脚手架 + 路由 + 布局 + Axios 封装
   │
   ▼
第2步：组长定义接口文档 + 数据库 SQL 发给组员
   │
   ▼
第3步：四人完全并行开发，互不阻塞
   │
   ├── 组长：核心复杂逻辑（值班管理、审核、8周占用、管理员端前端、助理端前端）
   ├── 闫鑫哲：学生端全栈（CRUD + 登记表 + 预约页）
   ├── 张奇奇：初访管理 + 通知服务 + 咨询师端前端
   └── 耿中显：统计分析 + 结案报告 + 统计前端
   │
   ▼
第4步：联调 → 交作业
```

> **关键原则**：组长骨架给到位 + 接口文档定义清晰 = 组员工作零依赖，各写各的，互不等。

---

## 七、接口规范约定

### 7.1 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 7.2 RESTful URL 规范

```
GET    /api/v1/{resource}          — 列表查询
GET    /api/v1/{resource}/{id}     — 详情查询
POST   /api/v1/{resource}          — 新增
PUT    /api/v1/{resource}/{id}     — 修改
DELETE /api/v1/{resource}/{id}     — 删除
```

### 7.3 微服务间调用

- 使用 OpenFeign 进行服务间调用，接口统一定义在 `common` 模块
- 所有服务在 Nacos 注册中心注册
- 组员在自己的服务中写业务即可，调用其他服务时直接注入 Feign 接口

---

## 八、数据库核心表（预设计）

| 表名 | 所属服务 | 说明 | 负责组员 |
|---|---|---|---|
| `sys_user` | user-service | 用户表（学生/初访员/助理/咨询师/管理员） | 闫鑫哲 |
| `counselor_info` | user-service | 咨询师/初访员基本信息 | 闫鑫哲 |
| `first_visit_form` | appointment-service | 首访登记表（含计分字段） | 闫鑫哲 |
| `first_visit_appointment` | appointment-service | 初访预约记录 | 闫鑫哲 |
| `duty_schedule` | appointment-service | 值班安排表 | 组长 |
| `time_config` | appointment-service | 时间段配置表 | 组长 |
| `first_visit_result` | consultation-service | 初访评估结果 | 张奇奇 |
| `consultation_appointment` | consultation-service | 咨询安排记录（含8周占用标记） | 组长 + 张奇奇 |
| `consultation_record` | consultation-service | 咨询记录（每次咨询状态） | 组长 |
| `consultation_extension` | consultation-service | 追加咨询时段申请 | 组长 |
| `closing_report` | consultation-service | 结案报告 | 耿中显 |
| `sms_log` | notification-service | 短信发送日志 | 张奇奇 |

---

## 九、Git 规范

- 前后端两个独立仓库，全员在 `main` 分支直接开发
- 提交前本地自测通过即可

---

## 十、交付物清单

| 序号 | 交付物 | 负责人 |
|---|---|---|
| 1 | 后端项目源代码 | 全员 |
| 2 | 前端项目源代码 | 全员 |
| 3 | 数据库设计文档（含 ER 图 + 建表 SQL） | 组长 |
| 4 | 接口文档（Swagger / Apifox） | 组长 |
| 5 | 部署说明文档 | 组长 |
| 6 | 课设结案报告 | 全员 |

---

> **文档版本**：v2.0  
> **制定日期**：2026-06-02  
> **制定人**：组长
